- route:
    autoStartup: false
    id: SampleTimer
    from:
      uri: "timer:SampleTimer?period=5s"
      steps:
        - log: 안녕 세계야!!! 지금 시각은 ${date:now:yyyy-MM-dd HH:mm:ss} ${variable.route:SampleTimer:DnaTemplateParameters}
