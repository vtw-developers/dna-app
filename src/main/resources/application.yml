spring:
  application:
    name: dna-app
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/portal
    username: vtw
    password: vtw123#
camel:
  variable:
    test: aaaa
  main:
    routes-include-pattern: "file:routes/**/*.yaml"
    shutdown-timeout: 1
  rest:
    binding-mode: json
  routetemplate:
    config:
management:
  security:
    enabled: false
  endpoints:
    web:
      exposure:
        include: "*"
