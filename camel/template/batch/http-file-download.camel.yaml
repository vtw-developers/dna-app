- route:
    autoStartup: true
    id: "HttpFileDownload"
    from:
      uri: "direct:HttpFileDownload"
      steps:
        - toD: "${variable.request.url}"
        - process:
            ref: FileNameProcessor
        - toD: "file:${variable.download.directory}"
