type: BATCH
id: HttpFileClient
name: http file download
parameters:
  request.url:
    type: string
    description: 요청 URL
  download.directory:
    type: string
    description: 다운로드 경로
  download.fileName:
    type: string
    description: 파일명
