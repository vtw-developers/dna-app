# 기본 정보
id: Api4
httpMethod: get
path: /service-2/api-4

# 부가 정보
tag: 서비스 둘
description: API 넷
requestParameters:
  - name: myName
    required: true
    dataType: string
  - name: myAddress
    required: false
    dataType: number
responseElements:
  - name: myName
    dataType: string
  - name: myAddress
    dataType: number

# 템플릿 정보
template:
  ref: Template_OpenApi_Relay
  parameters:
    relay:
      url: "http://localhost:8080/external/api-2"
